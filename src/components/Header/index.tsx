import React from "react";

import { Container } from "./styles";

const Header: React.FC = () => {
  return (
    <Container>
      <h1>React + TS ToDo</h1>
    </Container>
  );
};

export default Header;
